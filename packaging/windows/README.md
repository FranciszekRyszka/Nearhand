# Windows packaging

WiX / MSI definition, service registration, and the SAS policy the installer
sets so Ctrl+Alt+Del can be sent. Authenticode signing happens here too — it is
needed from the first public build against SmartScreen and antivirus false
positives (SignPath offers free signing for open source).

Scheduled for M3.
