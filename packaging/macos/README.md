# macOS packaging

PKG, LaunchDaemon (root) plus per-session LaunchAgent and a `LoginWindow` agent
for the login screen, entitlements, and the PPPC profile that lets MDM
pre-approve Screen Recording and Accessibility.

Developer ID, hardened runtime, notarization; one universal binary.

Scheduled for M4.
